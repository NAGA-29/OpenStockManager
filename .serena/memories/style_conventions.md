PHP code uses typed properties and typed method signatures where practical. Exception handling includes custom abstract base exceptions carrying context arrays and static factory constructors (for example `forDevice`, `analysisFailure`). Laravel code still follows older skeleton patterns like `app/Exceptions/Handler.php` and `bootstrap/app.php` singleton bindings. Keep edits ASCII by default and align with existing Laravel/PHP formatting conventions.
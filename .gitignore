# Monorepo root ignores
# Backend ignores are in api/.gitignore, frontend in frontend/.gitignore

# OS / IDE
/.idea
/.vscode
.DS_Store

# AI tooling
/.claude
/.serena
/.kiro
/.agents

# Docker override
docker-compose.override.yml

# Node (root-level, just in case)
node_modules
npm-debug.log
yarn-error.log
